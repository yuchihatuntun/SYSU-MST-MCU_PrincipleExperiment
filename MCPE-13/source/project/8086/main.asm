CODE    SEGMENT 
        ASSUME CS:CODE

IOCON	EQU 0b000H

START: 
		MOV AL,0ffH
		MOV DX,IOCON
OUTUP:  
		OUT DX,AL
		INC AL
		CMP AL,0ffh
		JE OUTDOWN
		JMP OUTUP
OUTDOWN:
		DEC AL
		OUT DX,AL
		CMP AL,00H
		JE OUTUP
		JMP OUTDOWN

CODE    ENDS
        END START
